{
  "framework": "nextjs",
  "buildCommand": "bun run build",
  "installCommand": "bun install --no-frozen-lockfile",
  "functions": {
    "src/app/cv/[pageId]/page.tsx": {
      "maxDuration": 60
    }
  }
}
